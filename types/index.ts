import { LucideIcon } from 'lucide-react';

export interface Post {
    id: string;
    title: string;
    content: string;
}

export interface AboutItem {
    icon: LucideIcon
    text: string
  }

  export interface Post {
    id: string;
    title: string;
    content: string;
  }
  